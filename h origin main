[1mdiff --git a/apps/web/src/app/api/leads/route.ts b/apps/web/src/app/api/leads/route.ts[m
[1mindex 1c621f8..19d34aa 100644[m
[1m--- a/apps/web/src/app/api/leads/route.ts[m
[1m+++ b/apps/web/src/app/api/leads/route.ts[m
[36m@@ -6,13 +6,11 @@[m [mexport async function POST(req: Request) {[m
     const body = await req.json();[m
     const supabase = await createClient();[m
 [m
[31m-    // Ricava l'utente autenticato[m
     const { data: { user }, error: authError } = await supabase.auth.getUser();[m
     if (authError || !user) {[m
       return NextResponse.json({ error: "Non autenticato" }, { status: 401 });[m
     }[m
 [m
[31m-    // Ricava l'agency_id dall'appartenenza dell'utente[m
     const { data: membership, error: memberError } = await supabase[m
       .from("agency_members")[m
       .select("agency_id")[m
[36m@@ -25,6 +23,12 @@[m [mexport async function POST(req: Request) {[m
       return NextResponse.json({ error: "Nessuna agenzia trovata" }, { status: 403 });[m
     }[m
 [m
[32m+[m[32m    const parseBudget = (v: unknown): number | null => {[m
[32m+[m[32m      if (v == null || v === "") return null;[m
[32m+[m[32m      const n = parseInt(String(v), 10);[m
[32m+[m[32m      return Number.isFinite(n) && n > 0 ? n : null;[m
[32m+[m[32m    };[m
[32m+[m
     const { data, error } = await supabase[m
       .from("leads")[m
       .insert([[m
[36m@@ -33,8 +37,8 @@[m [mexport async function POST(req: Request) {[m
           full_name:      body.full_name,[m
           email:          body.email ?? null,[m
           phone:          body.phone ?? null,[m
[31m-          budget_min:     body.budget_min != null && body.budget_min !== "" ? parseInt(String(body.budget_min), 10) || null : null,[m
[31m-budget_max:     body.budget_max != null && body.budget_max !== "" ? parseInt(String(body.budget_max), 10) || null : null,[m
[32m+[m[32m          budget_min_eur: parseBudget(body.budget_min),[m
[32m+[m[32m          budget_max_eur: parseBudget(body.budget_max),[m
           preferred_city: body.preferred_city ?? null,[m
           source:         body.source,[m
           status:         body.status || "new",[m
[36m@@ -55,4 +59,4 @@[m [mbudget_max:     body.budget_max != null && body.budget_max !== "" ? parseInt(Str[m
     console.error("[api/leads POST] unexpected:", err);[m
     return NextResponse.json({ error: "Internal server error" }, { status: 500 });[m
   }[m
[31m-}[m
[32m+[m[32m}[m
\ No newline at end of file[m
